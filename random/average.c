// Find the mean average of N given numbers, to two decimal places
#include<cs50.h>
#include<stdio.h>

int main(void)
{
    int total = 0;
    int i = 0
    while(total > 1)
    {
        total = get_int "Total numbers to average: \n";
    }
    int numbers[total];
    



int numfinder(int total, int x)


        int y = (x + 1)
        int z = get_int "Number %i:", y);
        x ++;
        return x, z;
    }
}


